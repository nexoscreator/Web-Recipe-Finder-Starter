import{fetchRecipes as e,displayRecipes as r}from"./searchForm.js";let searchForm=document.getElementById("search-form"),ingredient=document.getElementById("search-input"),loading=document.getElementById("loading"),progressBar=document.getElementById("progress-bar");function animateProgressBar(){progressBar.style.width="0";let e=0,r=setInterval(()=>{e>=100?clearInterval(r):(e+=10,progressBar.style.width=e+"%")},100)}function handleFormSubmit(){searchForm.addEventListener("submit",async t=>{t.preventDefault();let n=document.getElementById("recipe-results");n.innerHTML="",loading.style.display="flex",animateProgressBar();let i=ingredient.value;await e(i).then(e=>{loading.style.display="none",e?r(e):console.error("Failed to fetch recipes")}).catch(e=>{loading.style.display="none",console.error("Error fetching recipes:",e)})})}handleFormSubmit(),feedbackMessage.textContent="No recipes found. Please try different ingredients.",recipeContainer.appendChild(feedbackMessage);